<%= form_for(@ruleset) do |f| %>
  <% if @ruleset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ruleset.errors.count, "error") %> prohibited this ruleset from being saved:</h2>
      <ul>
      <% @ruleset.errors.full_messages.each do |msg| %>
        <%= content_tag(:li,  msg) %>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
